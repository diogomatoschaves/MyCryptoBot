POSTGRES_DB=
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
SECRET_KEY=
# Separate signing key for service JWTs (falls back to SECRET_KEY if unset)
JWT_SECRET_KEY=
BINANCE_API_KEY=
BINANCE_API_SECRET=
BINANCE_API_KEY_TEST=
BINANCE_API_SECRET_TEST=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET=
USE_CLOUD_STORAGE=

# --- Optional config overrides (shared/utils/settings.py). All have safe
# defaults matching the old proj.conf, so none is required. ---
# REDIS_URL=redis://localhost:6379
# LOGGER_LEVEL=INFO
# DEFAULT_MIN_ROWS=1000
# CHECK_INCONSISTENCIES=true
# RESTART_FAILED_PIPELINES=true
# RESTART_RETRIES=2
# TOKEN_EXPIRES_DAYS=3
# CHECKS_INTERVAL=3600
# BASE_CANDLE_SIZE=5m
# SNAPSHOTS_INTERVAL=300
